# A public Java method the blocking facade does not wrap.
# Type.method, then why. SurfaceSuite fails when this list drifts.
# Type.method(Types) names one overload of a method that is otherwise wrapped.

Client.name captured on info
Pane.active captured on info
Pane.currentCommand captured on info
Pane.currentPath captured on info
Pane.currentPathText captured on info
Pane.edges captured on info
Pane.floating captured on info
Pane.id captured on info
Pane.index captured on info
Pane.noteTyped records input written around the facade, which only a Java caller does
Pane.pid captured on info
Pane.position captured on info
Pane.size captured on info
Pane.title captured on info
Server.control Cats Control.attach fences the session; blocking callers use Java
Server.shell runShell and runShellCapturing cover it; if-shell stays on Java
Server.keys key bindings are configuration, set once through Java or tmux.conf
Server.commands the command catalog stays on Java
Server.messageLog the server message log stays on Java
Server.prompt the prompt history scope stays on Java
Server.toBuilder derives a new client; open a ServerConfig instead, or use within for a deadline
Session.attached captured on info
Session.id captured on info
Session.name captured on info
Window.active captured on info
Window.context captured on info
Window.id captured on info
Window.index captured on info
Window.layout captured on info
Window.linked captured on info
Window.name captured on info
Window.size captured on info
Pane.capture(Function1) builds a spec in a Java lambda; pass a built CaptureSpec
Pane.split(Function1) builds a spec in a Java lambda; pass a built SplitSpec
Server.newSession(Function1) builds a spec in a Java lambda; pass a built SessionSpec
Session.newWindow(Function1) builds a spec in a Java lambda; pass a built WindowSpec
Window.split(Function1) builds a spec in a Java lambda; pass a built SplitSpec
