# Kotlin's ABI Validation writer always ends this dump with a trailing blank line; that is the
# tool's own output format, regenerated by :libtmux-kotlin:updateKotlinAbi, not something to flag.
libtmux-kotlin/api/*.api whitespace=-blank-at-eof
